#include <stdio.h>

/**
 * main - entry point
 *
 * Description: this is the main function program
 * print from 0 -9
 * Return: is always 0
 */
int main(void)
{
int x;
for (x = 48; x < 58; x++)
putchar(x);
putchar('\n');
return (0);
}
