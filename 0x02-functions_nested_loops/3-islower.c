#include "main.h"
/**
 * 3-islower - function started here
 * Description: check the alphabet is islower 
 * Return:0
 */
int _islower(int x)
{
	if (x >= a && x <= 'z')
		return 1;
	else
		return 0;
 }