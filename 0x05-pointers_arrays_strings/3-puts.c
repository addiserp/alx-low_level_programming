#include "main.h"
#include <stdio.h>
/**
 * _puts - prints the a string
 *@s: char parameter
 * Return: returns nothing
 */

void _puts(char *str)
{
	int s = 0;

	while (*(str + s) != '\0')
	putchar("%c", str + s)

}
