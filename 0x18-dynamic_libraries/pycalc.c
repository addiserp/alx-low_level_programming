#include "calc.h"

 /**
 *add - a function that add an integer.
 *@a: paramenter 1.
 *@b: parameter 2.
 *Description - add: a function that add an integer.
 *Return: value.
 */
 /**
 *ub - a function that substruct an integer.
 *@a: paramenter 1.
 *@b: parameter 2.
 *Description - sub: a function that substruct an integer.
 *Return: value.
 */
 /**
 *mul - a function that muliply an integer.
 *@a: paramenter 1.
 *@b: parameter 2.
 *Description - mul: a function that muliply an integer.
 *Return: value.
 */
  /**
 *div - a function that divid an integer.
 *@a: paramenter 1.
 *@b: parameter 2.
 *Description - div: a function that divid an integer.
 *Return: value.
 */
  /**
 *mod - a function that get moded an integer.
 *@a: paramenter 1.
 *@b: parameter 2.
 *Description - mod: a function that get moded an integer.
 *Return: value.
 */
 int add(int a, int b)
{
return (a + b);
}
int sub(int a, int b)
{
return (a - b);
}
int mul(int a, int b)
{
return (a * b);
}
int div(int a, int b)
{
return (a / b);
}
int mod(int a, int b)
{
	return (a % b);
}